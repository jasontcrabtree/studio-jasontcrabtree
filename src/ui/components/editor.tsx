/**
 * @description Text editor with support for markdown. Can switch between different modes (blog post, code snippets)
 */
const Editor = () => {
    return <div>Editor</div>
}

export default Editor
